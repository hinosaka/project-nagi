# CHANGELOG（変更履歴）

仕様・実装の変更履歴を記録する。[Keep a Changelog](https://keepachangelog.com/) の形式を参考にする。

## 目次

- [記載ルール](#記載ルール)
- [Unreleased](#unreleased)

## 記載ルール

- 日付ベースで記録する（例：`## [2026-07-30]`）
- 変更種別：`Added` / `Changed` / `Fixed` / `Removed`
- 変更に関連するIDがあれば併記する：要件ID（REQ-XXX）／非機能要件ID（NFR-XXX）／画面ID（SCR-XXX）／シート名（DATABASE.md記載のシート名）

## [Unreleased]

### Added

### Changed

### Fixed

### Removed

## [2026-07-31]

### Added

- 伝票入力（SCR-002）を「会計処理」に改称し全面刷新：「会計管理」「清算」のトグルタブ構成に再設計。実装前にArtifactでモックアップを提示しユーザー承認を得て反映
  - 会計管理タブ：会計日の前後移動・カレンダー変更・天候ワンタップ保存・対象日合計金額を表示。既存伝票をコンパクトなカード表示（顧客・座席・人数・合計金額、削除アイコン付き）に変更。「伝票作成」ボタンまたは既存カードのタップで、一覧から独立した伝票作成／編集ビューに切り替わる（戻るボタンで一覧に戻る）。人数・座席（記号のみ）・顧客（オートコンプリート＋その場新規登録）・備考の入力、大分類×中分類のカテゴリ選択、商品一覧（丸型＋－ボタンによる数量選択）、登録外商品の直接入力（「＋その他ドリンク／フード」）を実装。確認ポップアップ（明細・合計金額・確定ボタン、確定後は伝票一覧に戻る）を新設
  - 清算タブ（新機能）を実装：売上情報（合計金額・会計数）、現金状況（準備金の入力・現金売上額・予想現金残高）、レジクローズの実行と実施履歴一覧を表示。レジクローズは会計管理をロックせず、同一日に複数回実行可能（画面：SCR-002、要件：REQ-005〜011, REQ-022, REQ-025, REQ-039〜041）
  - `BusinessDay`に`StartingCash`（準備金）列を追加、新規シート`RegisterCloseLog`（レジクローズ実施履歴）を追加。`CashRegisterService.gs`, `RegisterCloseLogRepository.gs`を新規作成。日次トランザクションIDの採番規則を`DailyIdRule`として共通化し`SalesIdRule`から分離（`RegisterCloseLog`と共用）
  - `shared/icons.html`に`icon-minus`を追加。サイドバーの表記を「伝票入力」→「会計処理」に変更
- ダッシュボード（SCR-006）を全面刷新：本日／昨日／過去7日間／当月／期間指定の期間ボタン、差分基準（前日比／前週同曜日比／前月同日比、単日選択時のみ）、集計対象日の表示、差分付きKPI（売上高・ドリンク/フード売上高・客単価・合計客数・合計組数・客席回転率）、売上推移グラフ（目盛り線付き縦棒グラフ）、商品別売上ランキングTOP10（数量順・2段組み）、カテゴリ別売上分布（円グラフ3種：全体比率・ドリンク内訳・フード内訳、実数値表併記）、一言アドバイス（内容に応じて配色変更）を実装（画面：SCR-006、要件：REQ-017, REQ-018, REQ-024, REQ-030, REQ-036, REQ-037, REQ-038）。実装前にArtifactでモックアップを提示しユーザー承認を得て反映
- `SalesDetail`に`CategoryMedium`列を追加：メニュー選択時は`Menu.CategoryMedium`のスナップショット、登録外（直接入力）の明細は中分類選択UIがないため空欄のまま（中分類別集計では「その他」として扱う）。ダッシュボードのカテゴリ別売上分布（ドリンク内訳・フード内訳）で使用（シート：SalesDetail）
- SPEC.mdに客席回転率の要件（REQ-038）を追加
- `shared/stylesheet.html`にカテゴリカルパレット（`--cat-1`〜`--cat-6`）を追加：円グラフ等、3系列以上の分類表示専用（状態表示のgood/warn/badとは別枠）。dataviz skillの検証済み参照パレットを採用し、実際のsurface色に対して検証済み
- 売上統計（新設）：予算管理（SCR-007）と月次推移・比較（SCR-011）を「予算実績」「月次推移」のタブ切替1画面に統合。対象月選択は両タブで共通化（画面：SCR-007, SCR-011）
- データ分析（新設）：顧客分析（SCR-009）と曜日・天候・座席分析（SCR-010）を「顧客」「曜日・天候・座席」のタブ切替1画面に統合。期間切り替えは両タブで共通化し、既定値は日次に統一（画面：SCR-009, SCR-010）
- `shared/icons.html`に`icon-trending-up`（売上統計用）・`icon-pie-chart`（データ分析用）を追加

- ダッシュボード（SCR-006）を実装：期間切り替え（日次／週次［月曜始まり］／月次［暦月］）、基準日選択（カレンダーアイコンボタン）と前後の期間へ移動するボタン、「期間：」ラベル付きの期間表示、KPI（売上高・客単価・来店組数・来店人数・フード売上・ドリンク売上）、フード/ドリンク構成比（構成比バー）、商品ランキング（フード／ドリンクそれぞれ売上金額上位5件を別枠表示）を表示。`getDashboardData()`をDashboardService.gsに追加（画面：SCR-006、要件：REQ-017, REQ-018, REQ-024, REQ-030、シート：Sales, SalesDetail）
- サイドバーにダッシュボードへのリンクを追加
- 日次／週次／月次の期間切り替えUI（タブ・前後ボタン・基準日入力）を`shared/javascript.html`の`createPeriodController()`として共通化
- フード/ドリンク構成比の凡例（`.legend`/`.dot`）を`shared/stylesheet.html`に共通化
- 予算管理（SCR-007）を実装：対象月選択（月ピッカー＋前月/翌月ボタン）、目標売上・備考の登録編集（フォーム化しEnterキーでも保存可能）、実績売上の自動集計、達成率（達成/未達で色分け）を表示。`BudgetService.gs`（`getBudgetData()`/`saveBudgetTarget()`）、`SalesTargetRepository.gs`を追加し、`setupDatabase()`にSalesTargetシートの作成を追加（画面：SCR-007、要件：REQ-027, REQ-028、シート：SalesTarget, Sales）
- 商品分析（SCR-008）を実装：期間切り替え、売上高・販売点数・平均注文点数、フード/ドリンク構成比（売上・点数）、商品別ランキング（売上・構成比・数量・粗利益・原価率・ABC分析、並び替え対応）を表示。`ProductAnalysisService.gs`（`getProductAnalysisData()`）を追加（画面：SCR-008、要件：REQ-018, REQ-029, REQ-030, REQ-031, REQ-034、シート：SalesDetail, Menu）
- 顧客分析（SCR-009）を実装：期間切り替え、常連率・新規来店・リピート来店、新規/リピート内訳（構成比バー）を表示。新規/リピート判定はCustomer.VisitCountを使わずSales.CustomerId×Sales.SalesDateから来店順を都度算出する方式とした（DATABASE.md 5章準拠）。`CustomerAnalysisService.gs`（`getCustomerAnalysisData()`）を追加（画面：SCR-009、要件：REQ-020, REQ-032、シート：Sales, Customer）
- 曜日・天候・座席分析（SCR-010）を実装：期間切り替え（既定は週次）、曜日別の売上・来店組数・来店人数・客単価（棒グラフ＋表）、天候別の売上・来店組数、座席種別ごとの利用件数・稼働率を表示。`DayWeatherSeatAnalysisService.gs`（`getDayWeatherSeatAnalysisData()`）を追加（画面：SCR-010、要件：REQ-019, REQ-021、シート：BusinessDay, Sales, Seat）
- 月次推移・比較（SCR-011）を実装：対象月選択（月ピッカー＋前月/翌月ボタン）、当月実績・前月比・前年同月比（増減で色分け）、直近12ヶ月の売上推移（棒グラフ）を表示。`MonthlyTrendService.gs`（`getMonthlyTrendData()`）を追加（画面：SCR-011、要件：REQ-033、シート：Sales）
- ダッシュボードに、予算管理・商品分析・顧客分析・曜日/天候/座席分析・月次推移の5画面への遷移リンク（「詳細分析」カード）を追加。各分析画面には「ダッシュボードに戻る」リンクを追加

- 座席管理（SCR-005）を実装：店内レイアウト（テーブルTB/TA・カウンターC1〜C8）に基づく配置図での一覧と、切替メニュー内の「無効にする」「有効にする」ボタンによる使用可否切替（誤操作防止のため座席クリックでは切り替わらない）。座席番号・種別・定員は固定のため編集・新規追加は対象外（画面：SCR-005、シート：Seat）
- `setupDatabase()`を拡張し、Seatシートの作成と初期データ（C1〜C8、TA、TB）の投入に対応。未作成のシートだけを追加する形にリファクタリングし、再実行可能にした（シート：Seat）
- 顧客管理（SCR-004）を実装：一覧表示（ニックネーム・来店回数・最終来店日・初回来店日）、ニックネームのみの新規登録、一覧のニックネームクリックで開く詳細ポップアップ（電話番号・過去注文の多いメニュー・メモの表示、電話番号・メモの編集）。新規登録時は来店回数0・初回/最終来店日を登録日時で初期化し、伝票入力の会計登録で加算・更新される（画面：SCR-004、要件：REQ-012, REQ-014、シート：Customer）
- Customerに自由記述の`Memo`列を追加（シート：Customer）
- マスタ系ID（`{頭文字}-{4桁連番}`）の採番規則を`SequentialIdRule`として共通化し、Menu・Customerで共用（旧`MenuIdRule`を統合）（ARCHITECTURE.md準拠）
- `setupDatabase()`にCustomerシートの作成を追加（データは投入せず、実際の登録画面から入力する運用）（シート：Customer）
- 伝票入力（SCR-002）を実装：会計日ごとの天候入力（絵文字ボタンでワンタップ保存）、対象日の会計一覧（編集・削除）、会計登録フォーム（人数・座席・顧客・明細）。明細はメニューを大分類（ドリンク上段／フード下段）×中分類ごとのタップ式ボタンから素早く追加でき、数量は＋－ボタンで調整。メニュー外の明細は「その他ドリンク（直接入力）」「その他フード（直接入力）」等、大分類別の選択肢から入力（商品名・単価は自由入力）。顧客はオートコンプリート検索＋候補になければ保存時にその場で新規登録。会計の保存・削除のたびに、関連する顧客のVisitCount・LastVisitDateをSales実データから再計算する（画面：SCR-002、要件：REQ-005〜011, REQ-022, REQ-025、シート：Sales, SalesDetail, BusinessDay）
- SalesDetailに`CategoryLarge`列を追加。メニュー選択時はMenuのスナップショット、その他（直接入力）選択時は店主が選んだ大分類を保存し、登録外の明細もカテゴリ別集計の対象にする（シート：SalesDetail）
- 伝票入力の合計金額表示の近くに、客単価・フード売上金額・ドリンク売上金額のリアルタイム表示を追加（画面：SCR-002）
- `setupDatabase()`にSales・SalesDetail・BusinessDayシートの作成を追加（シート：Sales, SalesDetail, BusinessDay）
- SheetUtilに物理削除用の`deleteRowsByColumnValue`を追加（Sales/SalesDetailは論理削除ではなく実削除の方針のため）
- 共通デザインシステムを`shared/stylesheet.html`に整備（CSS変数による配色・spacing・radius、`.app-shell`/`.sidebar`/`.card`/`.badge`/`.kpi-tile`等の共通コンポーネント）。参考デザイン（design-reference/、BankDash UIキット）を基に、ユーザー承認を得たうえで既存5画面（ホーム・伝票入力・メニュー・座席・顧客）に適用した
- 左サイドバーナビゲーション（`shared/sidebar.html`）を追加し、全画面共通で表示。`include()`をテンプレート変数を渡せる形に拡張（`ScriptApp.getService().getUrl()`によるbaseUrl等をsidebar内のリンクにも反映）
- 方針をUI.md 5章に記載（デザイン変更は実装前にモックアップで提案し承認を得てから反映する運用も明記）
- 絵文字を単色SVGアイコン（`shared/icons.html`、`currentColor`で文字色に追従）に置き換え。外部フォント・CDNには依存しない
- サイドバーに狭幅画面（スマホ等）向けのハンバーガーメニュー開閉を追加
- 補足説明用のポップオーバー共通部品（`.info-btn`＋`.popover`）を追加
- 伝票入力（SCR-002）のUIを改善：テーブル罫線を横線のみ＋縞模様に簡素化、会計日欄に曜日を表示（天候欄からは削除）、明細のクイック選択ボタンサイズを統一、数量＋－ボタンに着色、明細行追加ボタンをテーブル内に配置、顧客欄の説明書きをinfoアイコン＋ポップオーバーに変更、フード/ドリンク売上をアイコン＋色分け表示、人数・単価入力をテンキーパッド対応、保存ボタンをセンタリング、テーブルのはみ出し防止（横スクロール対応）
- 共通のタブ切り替えコンポーネント（`.tabs`/`.tab`）を追加
- メニュー管理（SCR-003）のUIを改善：登録フォームの見出しをカード外に移動し「メニュー追加」に改称、入力欄の並びを大分類・中分類・商品名・価格・原価に変更、新規登録後は大分類・中分類を保持して連続入力しやすく、価格・原価入力をテンキーパッド対応、登録ボタンをセンタリング、商品一覧を大分類・中分類でソートしフード/ドリンクのタブで絞り込めるように変更
- メニュー管理の大分類・中分類をプルダウン化（既存の値から選択、または「＋新規入力」でその場入力）。タブにドリンク/フードの色付きアイコンと件数を表示、商品一覧から冗長な大分類・中分類列を削除し中分類ごとにグルーピング表示（グループ見出しにも件数を表示）
- 座席管理（SCR-005）：テーブル（TA/TB）の座席ボックスをカウンター座席より大きく表示し、定員の違いが視覚的に分かるように変更
- 座席管理（SCR-005）のUIを改善：カウンター座席が狭幅画面でも横1列を維持（はみ出し分は横スクロール）、選択中の座席を枠線＋着色で表示、無効な座席はボタン全体をグレーアウト、「閉じる」ボタンを廃止し外側クリックで閉じる方式に変更、「無効にする」「有効にする」ボタンをそれぞれ色分け
- 顧客の注文ランキング取得（`getCustomerOrderRanking`）を追加：SalesDetail実データからよく注文する商品TOP3とそれ以外を集計して返す（画面：SCR-004、シート：SalesDetail）
- 顧客管理（SCR-004）のUIを改善：新規登録をニックネーム欄とボタンを1行に統合し見出しを削除、ボタン文言を「新規登録」に変更、一覧のニックネームを太字・下線なしのリンクにしinfoアイコンで補足説明、詳細ポップアップを人物アイコン付きで①ニックネーム②来店回数・前回来店日③電話番号④メモ（タグアイコン＋インライン保存ボタン）⑤よく注文する商品TOP3⑥その他の注文⑦「顧客一覧に戻る」（センタリング）の順に再構成し、領域外クリックでも閉じるように変更
- 顧客管理の詳細ポップアップをさらに改善：来店回数・前回来店日をそれぞれ色付きバッジ表示、電話番号ラベルをアイコンに変更し入力欄の罫線を削除、編集ボタン（右配置）で入力欄にフォーカスする方式に変更。「顧客一覧に戻る」ボタンのセンタリングが共通スタイル未定義で効いていなかった不具合を修正（`.form-actions`をshared/stylesheet.htmlに集約）
- 伝票入力（SCR-002）のUIをさらに改善：会計日欄は日付表示が重複していたため入力欄の文字を非表示にしカレンダーアイコンのみに変更（曜日表示は維持）、フード/ドリンク売上の並びを逆転、数量の＋－ボタンをそれぞれ色分け（－は警告色、＋は好調色）、数量入力欄（＋－操作のみのため）の罫線を削除

### Changed

- 使いやすさ向上のため、サイドバーメニューの並び順・構成を再編：ダッシュボード／伝票入力／商品管理／売上統計／商品統計／データ分析／顧客管理／座席管理の順に変更（ユーザーフィードバックに基づく）
- トップURL（`page`パラメータなし）のデフォルト遷移先を、ホーム画面からダッシュボード（SCR-006）に変更
- メニュー管理（SCR-003）を「商品管理」に改称（機能は変更なし）
- 商品分析（SCR-008）を「商品統計」に改称（機能は変更なし）
- ダッシュボードの「詳細分析」カードのリンクを、統合後の3画面（売上統計・商品統計・データ分析）へのリンクに更新
- SPEC.mdのREQ-036・REQ-037（売上サマリー・一言アドバイス、未実装）の表示先を「ホーム画面」から「ダッシュボード画面」に変更（機能は将来ダッシュボードに実装する方針）
- 座席管理（SCR-005）の無効な座席から取り消し線を削除（背景色のグレーアウトのみで表現）
- 顧客管理（SCR-004）の詳細ポップアップで、メモ欄を電話番号欄と同じ罫線なし＋アイコンボタン形式に統一（保存ボタンをチェックアイコンの`.icon-btn`に変更）
- メニュー管理（SCR-003）の登録フォームで、大分類・中分類を「カテゴリ」として1行にまとめ「大分類 > 中分類」の並びで表示。価格・原価ラベルの「（円）」表記を削除

### Fixed

- メニュー管理（SCR-003）の商品一覧テーブルの縞模様配色が見づらかったため、白背景に統一

- 顧客管理（SCR-004）で一覧が表示されない不具合を修正：`getCustomerList()`の失敗が画面に表示されず無言で終わっていたためエラー表示を追加。あわせて`FirstVisitDate`/`LastVisitDate`を`Date`型のまま`google.script.run`で返していたのをやめ、サーバー側で文字列（`yyyy/MM/dd`）に整形してから返すよう変更（画面：SCR-004）
- メニュー管理画面（SCR-003）が白紙表示になる不具合を修正：`include()`に渡すファイル名を`shared/stylesheet`から`client/shared/stylesheet`に訂正（GAS上のファイル名は`rootDir`からの相対パスであり、画面ファイルからの相対パスではないため）。ARCHITECTURE.mdの記載例も合わせて訂正（画面：SCR-003）
- ホーム画面・各画面間の遷移リンクが正しく機能しない不具合を修正：Webアプリの`/exec` URLはアクセス時に`googleusercontent.com`のURLへリダイレクトされ、相対パスのリンク（`href="?page=xxx"`）が正しく解決できなかった。`doGet`で発行する絶対URL（`ScriptApp.getService().getUrl()`）を各画面に埋め込み、リンクを絶対URL化した（ARCHITECTURE.mdに注意点を追記）
- 伝票入力（SCR-002）で、会計日の多い日に画面表示が遅くなる不具合を修正：明細取得が会計件数ぶんSalesDetail全体を読み直すN+1状態になっていたため、1回の取得結果をグルーピングする方式に変更
- 伝票入力（SCR-002）の明細で、「その他ドリンク」⇔「その他フード」など「その他」系の選択肢を切り替えると、手入力済みの商品名・単価が消えてしまう不具合を修正（実メニューからその他への切り替え時のみクリアするよう変更）
- 伝票入力（SCR-002）で、入力中に会計日を変更すると内容が消えてしまう問題を修正：入力中の内容がある場合はフォームを維持したまま一覧・天候だけ選択した日付のものに切り替え、フォームが対象としている日付をラベルで明示するようにした。保存もフォームが対象とする日付で行われる（それまでは確認ダイアログで警告するだけだった）

### Removed

- ホーム画面（SCR-001）を廃止：ナビゲーション専用のプレースホルダー画面で実質的な機能を持たなかったため。トップURLの遷移先はダッシュボード（SCR-006）に変更（画面：SCR-001）
- 予算管理（SCR-007）・月次推移比較（SCR-011）・顧客分析（SCR-009）・曜日天候座席分析（SCR-010）の単独画面を削除（それぞれ売上統計・データ分析に統合）

## [2026-07-30]

### Added

- 売上管理のデータ構造を新規設計（シート：Menu, Sales, SalesDetail）
- 新規／リピート判別のためCustomerシートを新設（シート：Customer）
- 曜日・天候を分析するためBusinessDayシートを新設（シート：BusinessDay）
- 客単価・席稼働分析のためSeatシートを新設し、Salesに`PartySize`・`SeatId`を追加（シート：Seat, Sales）
- SalesDetailに`CustomerId`を追加し、顧客ごとのメニュー傾向を集計できるようにした（シート：SalesDetail）
- Menuに2階層カテゴリ`CategoryLarge`/`CategoryMedium`を追加（シート：Menu）
- SPEC.mdの非機能要件にID（NFR-XXX）と優先度区分を付与
- UI.mdの画面別仕様テンプレートに「使用データ」項目を追加、顧客選択UXのメモを追加
- ホーム画面（SCR-001）に、直近営業日の売上サマリーと実績ベースの一言アドバイス機能を追加（画面：SCR-001、要件：REQ-036, REQ-037）
- UI.mdにSCR-001〜SCR-011の全11画面（ホーム〜月次推移・比較）の詳細仕様（目的・表示項目・使用データ・操作・画面遷移）を記載（UI.md）
- ARCHITECTURE.mdにレイヤー構成（UI層／アプリケーション層／ドメイン層／データアクセス層）・ディレクトリ構成・層間の依存ルールを追加（ARCHITECTURE.md）
- 天候に基づく事前アドバイスの実現方式（店主の朝入力運用／外部天気API）をTODOとしてDATABASE.mdに追加（シート：BusinessDay）
- 開発環境を構築：`clasp`導入、GASプロジェクト新規作成（standalone、`rootDir: src`）、DEVELOP.mdにセットアップ手順を記載
- ARCHITECTURE.mdのディレクトリ構成に沿って`src/client/`（11画面分のHTMLひな形＋`shared/`）・`src/server/`（`main.gs`、`application/`・`domain/`・`repository/`）を作成（中身は未実装）
- Webアプリの公開設定を決定：アクセス可能なユーザーは「全員（Googleアカウントでのログイン必須）」、実行ユーザーは「自分（デプロイしたアカウント）」（ARCHITECTURE.md, `src/appsscript.json`のwebapp設定）
- `main.gs`に疎通確認用の暫定`doGet`を実装し、Webアプリとして初回デプロイ（DEVELOP.mdにデプロイ手順・バージョン管理方針を記載）
- メニュー管理（SCR-003）を実装：一覧表示・新規登録・編集・販売終了（論理削除）（画面：SCR-003、要件：REQ-001, REQ-002, REQ-003, REQ-004, REQ-026、シート：Menu）
- 全リポジトリ共通のデータアクセス基盤を追加：スプレッドシートID保持（Script Properties）、初回セットアップ用`setupDatabase()`、シート⇔オブジェクト変換の共通処理（`SheetUtil`）、ページルーティング（`main.gs`の`page`パラメータ）をARCHITECTURE.mdに追記

### Changed

- `Sales.SalesDateTime`を`SalesDate`に変更し、時刻を持たない仕様にした（シート：Sales）
- 顧客の識別方式を「店主が入力するニックネーム」に変更し、電話番号は識別に使わず連絡先情報として扱う方針にした（シート：Customer）
- 「会計入力」の表記を「伝票入力」に統一（SPEC.md, UI.md）
- アプリケーション名を「pos-app」から「店長のノート」に変更（Webアプリのタイトル、ホーム画面表示、GASプロジェクト名、スプレッドシート名）

### Removed

- `Sales.PaymentMethod`を削除（支払いは現金固定のため）（シート：Sales）
