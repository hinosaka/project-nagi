# 目的

- 小規模飲食店向けの「店主専用アプリ」をGoogle Apps Script（GAS）で開発する
- 店主が日々の業務（売上・在庫・注文などの管理）をPC・iPhoneのブラウザから行えるようにする
- Googleスプレッドシートを唯一のデータベース（Single Source of Truth）とし、長期運用・保守性・拡張性を優先したレイヤードアーキテクチャで実装する

# やらないこと

- 顧客向け機能（予約・注文・決済などのお客様が直接触るUI）は対象外
- 複数スタッフでの同時利用・権限管理は対象外（店主一人が使う前提）
- 店主によるスプレッドシートの直接編集は想定しない（アプリ経由のみでデータ操作する）
- 外部決済・会計サービスとの連携は対象外（Future検討事項として扱う）
- スプレッドシート以外のデータベース（Firestore等）への移行は行わない

# 開発姿勢

- 実装より設計品質を優先する
- 設計に改善余地がある場合、実装前に必ず代替案を提示する
- 指示をそのまま実装せず、保守性・拡張性・可読性の高い設計があれば積極的に提案する

# 用語

| 用語 | 意味 |
|---|---|
| 店主 | このアプリの唯一の利用者。スプレッドシートを直接操作しない |
| Decision | ユーザーがChatGPTと要件定義・設計を進めた結果として共有される決定事項 |
| Single Source of Truth | Googleスプレッドシートを唯一の正とするデータ管理方針 |
| Must / Should / Future | 要件の優先度区分（必須／推奨／将来対応） |
